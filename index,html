<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Design Vault</title>
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: #f2f3f5;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 1200px;
      margin: 30px auto;
      background: #fff;
      border-radius: 16px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.08);
      padding: 20px;
    }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 10px;
    }
    .logo {
      font-weight: 700;
      font-size: 22px;
    }
    .search-bar {
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .search-bar input {
      padding: 8px 14px;
      border: 1px solid #ccc;
      border-radius: 25px;
      outline: none;
      width: 250px;
    }
    .search-bar button {
      padding: 8px 16px;
      border: none;
      border-radius: 25px;
      background: black;
      color: #fff;
      cursor: pointer;
    }
    .actions {
      display: flex;
      align-items: center;
      gap: 15px;
    }
    .actions button {
      padding: 8px 16px;
      border: none;
      border-radius: 25px;
      cursor: pointer;
      font-weight: 500;
    }
    .signin { background: none; }
    .signup { background: black; color: #fff; }
    .avatar {
      width: 35px;
      height: 35px;
      border-radius: 50%;
      background: #90d6f8;
    }

    /* Nav Tabs */
    .tabs {
      display: flex;
      gap: 15px;
      margin: 20px 0;
      flex-wrap: wrap;
    }
    .tabs button {
      padding: 8px 18px;
      border: none;
      border-radius: 25px;
      cursor: pointer;
      background: #f0f0f0;
    }
    .tabs button.active {
      background: #90d6f8;
    }

    /* Content Grid */
    .grid {
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: 20px;
    }
    .left, .right {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    .card {
      background: #fff;
      border-radius: 12px;
      padding: 20px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    .card img {
      width: 100%;
      border-radius: 12px;
    }
    .card h3 { margin: 10px 0; }

    .btn {
      display: inline-block;
      padding: 8px 16px;
      border-radius: 25px;
      background: black;
      color: #fff;
      text-decoration: none;
      font-size: 14px;
    }

    /* Popular Items */
    .popular {
      display: flex;
      gap: 10px;
      overflow-x: auto;
    }
    .popular img {
      width: 80px;
      height: 100px;
      object-fit: cover;
      border-radius: 12px;
    }

    /* New Arrivals */
    .arrivals {
      display: grid;
      grid-template-columns: repeat(auto-fit,minmax(200px,1fr));
      gap: 15px;
    }
    .arrival-card {
      background: #fff;
      border-radius: 12px;
      padding: 12px;
      text-align: center;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    .arrival-card img {
      width: 100%;
      border-radius: 8px;
      margin-bottom: 10px;
    }
    .price {
      font-weight: bold;
      margin: 5px 0;
    }
    .soldout {
      color: red;
      font-size: 12px;
    }

    @media (max-width: 900px) {
      .grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <div class="logo">DESIGN<br>VAULT</div>
      <div class="search-bar">
        <input type="text" placeholder="Search">
        <button>Search</button>
      </div>
      <div class="actions">
        <button class="signin">Sign In</button>
        <button class="signup">Sign Up</button>
        <div class="avatar"></div>
      </div>
    </div>

    <!-- Tabs -->
    <div class="tabs">
      <button>Table</button>
      <button>Lamp</button>
      <button>Speakers</button>
      <button class="active">Dresses</button>
      <button>Plants</button>
      <button>Bed</button>
    </div>

    <!-- Main Grid -->
    <div class="grid">
      <!-- Left -->
      <div class="left">
        <div class="card">
          <h3>MODERN LOOK & CLEAN STYLE</h3>
          <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit.</p>
          <a href="#" class="btn">Buy Now</a>
        </div>
        <div class="card">
          <h3>New Arrivals</h3>
          <div class="arrivals">
            <div class="arrival-card">
              <img src="https://via.placeholder.com/150" alt="">
              <p>Colorful Dress</p>
              <p class="price">$95.50</p>
              <p class="soldout">Sold Out</p>
            </div>
            <div class="arrival-card">
              <img src="https://via.placeholder.com/150" alt="">
              <p>Colorful Dress</p>
              <p class="price">$59.50</p>
              <p class="soldout">Sold Out</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Right -->
      <div class="right">
        <div class="card">
          <h3>Popular Items</h3>
          <div class="popular">
            <img src="https://via.placeholder.com/80x100" alt="">
            <img src="https://via.placeholder.com/80x100" alt="">
            <img src="https://via.placeholder.com/80x100" alt="">
            <img src="https://via.placeholder.com/80x100" alt="">
          </div>
        </div>
        <div class="card">
          <h2>THE BEST LOOK ANYTIME ANYWHERE</h2>
          <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit.</p>
          <a href="#" class="btn">Buy Now</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
